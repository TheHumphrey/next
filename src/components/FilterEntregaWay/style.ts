import styled from 'styled-components';

const FilterEntregaWayStyle = styled.div`
  .dropdownContainer {
    display: flex;
    justify-content: space-between;
    align-items: center;

    width: 35rem;
    height: 4rem;

    border: solid 1px rgba(0, 0, 0, 0.1);
    border-radius: 10px;
  }

  .dropdrownPages {
    left: 5px;
  }

  .dropdrownGroup {
    right: 5px;
  }
`;

export default FilterEntregaWayStyle;
