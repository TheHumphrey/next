import styled from 'styled-components';

const ButtonTableStyle = styled.div`
  .buttonContainer {
    display: flex;
    justify-content: center;
    align-items: center;

    width: 25rem;
    height: 4rem;

    border: solid 1px rgba(0, 0, 0, 0.1);
    border-radius: 10px;
  }
`;

export default ButtonTableStyle;
