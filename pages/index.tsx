import React from 'react';

import { Layout, GridDashboard } from '@app/components';

const IndexPage = (): React.ReactElement => (
  <Layout title="Portal - WAYDS">
    <GridDashboard />
  </Layout>
);

export default IndexPage;
